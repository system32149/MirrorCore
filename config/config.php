<?php
/**
 * Replaces the search result's amount of
 * levels to 9999, like on the GD servers
 *
 * If false, it will use the amount of
 * levels from the search paramaters
 */
$realLevelCount = true;

/**
 * Enables the rate button. The button can
 * let anyone to rate any level's
 * difficulty.
 *
 * Disabled by default because of it being
 * a bad idea.
 */
$rateGJLevel = false;
?>
